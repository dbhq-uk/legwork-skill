# Deep Research Skill Dependencies (Bright Data CLI)
#
# Required Python packages (installed by setup.sh into .venv):
#   (none — the SERP/Unlocker backend now shells out to the Bright Data CLI)

# Optional Python packages:
#   weasyprint — PDF generation from HTML reports.
#                Install: pip install weasyprint
#                Used by: reference/html-generation.md (Phase 8 PDF output)
#
# External tooling (not Python packages):
#   Bright Data CLI (`brightdata` / `bdata`, npm: @brightdata/cli)
#     - Install: npm install -g @brightdata/cli
#     - Authenticate: brightdata login   (or set BRIGHTDATA_API_KEY)
#     - Used by: scripts/bd_search.py
