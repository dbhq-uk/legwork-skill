# OS cruft
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local agent tooling state
.serena/

# Python
__pycache__/
*.pyc
.pytest_cache/

# The skill needs no virtualenv (stdlib only), but do not commit one if a
# contributor makes one anyway.
.venv/

# Research output: the skill's default output base is <git-root>/docs/research,
# so a run inside this repo would otherwise try to commit its own deliverables.
/docs/research/
